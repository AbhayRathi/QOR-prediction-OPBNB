{
  "name": "qor-contracts",
  "version": "1.0.0",
  "description": "QOR Network Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network opbnbTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network opbnbMainnet",
    "verify": "hardhat run scripts/verify.js --network opbnbTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1"
  }
}
